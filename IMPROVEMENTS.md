# 개선 방향 (Improvement Roadmap)

> 이 문서는 **현재 구현의 한계와 앞으로의 개선 계획**을 정직하게 정리한 것입니다.
> (현재 코드는 2024년 전공 프로젝트 결과물 그대로이며, 아래 개선은 **환경이 갖춰지면 진행할 후속 작업**입니다.)

---

## 배경 — 왜 파일(txt) 기반인가

당초 설계 목표는 **MySQL 기반 RDB 학사 시스템**이었고, `mysql-connector-j` 커넥터까지 받아 두었습니다.
하지만 DB 설치·구동 단계에서 막혀, 최종 제출은 **파일(txt) 기반 저장/로드**까지 구현했습니다.
→ 즉 RDB는 *목표였으나 미완*이며, 이 한계가 가장 분명한 개선 포인트입니다.

---

## 1순위 — 파일 → MySQL 마이그레이션 (RDB 실증)

현재 각 도메인 클래스가 `students.txt` 같은 파일을 직접 읽고 `split(",")`로 파싱합니다.
이를 관계형 스키마로 옮기면 동시성·무결성·검색 효율을 모두 개선할 수 있습니다.

- **스키마 설계**: `student`, `professor`, `employee`, `course`, `enrollment`, `course_history`, `counsel` 테이블 + 외래키(수강신청 → 학생/강의).
- **DAO 계층 분리**: 현재 9~10개 파일에 흩어진 파일 I/O를 `StudentDao` / `CourseDao` 등 **데이터 접근 인터페이스**로 모아, 저장소(파일↔DB) 교체가 쉬운 구조로.
- **트랜잭션**: 수강신청처럼 여러 테이블이 함께 바뀌는 작업을 트랜잭션으로 묶어 원자성 보장.
- **마이그레이션 스크립트**: 기존 txt → DB INSERT 변환 1회성 스크립트.

> ✅ 이 과정은 같은 포트폴리오의 *Smart Farm* 프로젝트에서 적용한 **"한계를 사후에 개선·검증"** 패턴과 동일하게, 원본은 보존하고 개선 모듈을 추가하는 방식으로 진행할 예정입니다.

## 2순위 — 보안

- **비밀번호 평문 저장 → 해시(BCrypt 등)**. 현재 비밀번호가 평문으로 저장되어 있어, 단방향 해시 + salt로 교체.
- 로그인 시 해시 비교로 변경.

## 3순위 — 코드 품질

- 중복된 파일 I/O 로직 공통화(위 DAO와 연계).
- 입력 검증·예외 처리 보강(빈 값·형식 오류·중복 학번 등).
- Swing UI 레이아웃을 절대좌표(`setBounds`)에서 레이아웃 매니저로 정리(반응형).

---

## 진행 상태

| 항목 | 상태 |
|---|---|
| MySQL 스키마 설계 | ⏳ 예정 (환경 세팅 후) |
| DAO 계층 분리 | ⏳ 예정 |
| 비밀번호 해시 | ⏳ 예정 |

> 진행 시 이 표와 함께 `improvements/` 모듈을 추가하고, **실제 구동·검증 결과**를 정직 라벨과 함께 기록할 계획입니다.
